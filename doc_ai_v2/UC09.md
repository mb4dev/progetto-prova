# UC09 - Acquisto Abbonamento

## Diagramma delle Classi

Vedi [Architettura Base](Architettura_Base.md) per le classi comuni.

### Backend

```mermaid
classDiagram
    class SubscriptionController {
        -service: SubscriptionService
        +resolveAction(action: string)
        -getSubscriptions()
        -purchase()
    }
    SubscriptionController --|> Controller

    class SubscriptionService {
        <<interface>>
        +getAvailableSubscriptions() SubscriptionType[]
        +purchaseSubscription(user: User, typeId: int) boolean
    }
    SubscriptionService --|> Service

    class DefaultSubscriptionService {
        -repository: SubscriptionRepository
        +getAvailableSubscriptions() SubscriptionType[]
        +purchaseSubscription(user: User, typeId: int) boolean
    }
    DefaultSubscriptionService --|> SubscriptionService

    class SubscriptionRepository {
        <<interface>>
        +findAllTypes() SubscriptionType[]
        +saveUserSubscription(sub: UserSubscription) boolean
        +hasActiveSubscription(userId: int) boolean
    }
    SubscriptionRepository --|> Repository
    
    DefaultSubscriptionService --> SubscriptionRepository
    SubscriptionController --> SubscriptionService
```

### Frontend

```mermaid
classDiagram
    class SubscriptionListView {
        -cardContainer: HTMLElement
        +displaySubscriptions(subs: SubscriptionType[])
        +bindPurchase(handler: function)
    }
    SubscriptionListView --|> View

    class SubscriptionPresenter {
        -view: SubscriptionListView
        -api: APIService
        +init()
        +purchase(typeId: string)
    }
    SubscriptionPresenter --|> Presenter
    
    SubscriptionPresenter --> SubscriptionListView
```

## Diagramma di Attività

```mermaid
flowchart TD
    Start([Inizio]) --> Login[Utente Autenticato]
    Login --> SelectSub[Menu Abbonamenti]
    SelectSub --> RenderView[Mostra Abbonamenti]
    RenderView --> SelectItem[Seleziona Pacchetto]
    SelectItem --> CheckActive{Già Attivo?}
    CheckActive -- Si --> Info[Mostra Info]
    Info --> RenderView
    CheckActive -- No --> Payment[Pagamento (UC08)]
    Payment --> PayResult{Pagamento OK?}
    PayResult -- No --> Error
    PayResult -- Si --> Activate[Attiva Abbonamento]
    Activate --> Confirm[Mostra Conferma]
    Confirm --> End([Fine])
```

## Diagramma di Sequenza

### Frontend

```mermaid
sequenceDiagram
    autonumber
    actor User
    participant View as SubscriptionListView
    participant Presenter as SubscriptionPresenter
    participant API as APIService
    participant Backend

    User->>View: Seleziona "Acquista"
    View->>Presenter: purchase(typeId)
    Presenter->>API: purchaseSubscription(typeId)
    API->>Backend: POST /subscription/purchase
    Backend-->>API: {success: true}
    API-->>Presenter: success
    Presenter->>View: showSuccess()
```

### Backend (Purchase)

```mermaid
sequenceDiagram
    autonumber
    participant Router
    participant Controller as SubscriptionController
    participant Service as SubscriptionService
    participant Repo as SubscriptionRepository
    participant DB as Database

    Router->>Controller: resolve("purchase")
    Controller->>Service: purchaseSubscription(user, typeId)
    Service->>Repo: hasActiveSubscription(userId)
    Repo-->>Service: false
    Service->>Repo: saveUserSubscription(sub)
    Repo->>DB: INSERT INTO user_subscriptions ...
    DB-->>Repo: success
    Repo-->>Service: true
    Service-->>Controller: success
    Controller-->>Router: Response 201
```
