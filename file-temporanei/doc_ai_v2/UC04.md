# UC04 - Prenotazione Campo Sportivo

## Diagramma delle Classi

Vedi [Architettura Base](Architettura_Base.md) per le classi comuni.

### Backend

```mermaid
classDiagram
    class BookingController {
        -service: BookingService
        +resolveAction(action: string)
        -createBooking()
    }
    BookingController --|> Controller

    class BookingService {
        <<interface>>
        +createBooking(user: User, slot: Slot) boolean
    }
    BookingService --|> Service

    class DefaultBookingService {
        -repository: BookingRepository
        -paymentService: PaymentService
        +createBooking(user: User, slot: Slot) boolean
    }
    DefaultBookingService --|> BookingService

    class BookingRepository {
        <<interface>>
        +save(booking: Booking) boolean
        +isSlotFree(slot: Slot) boolean
    }
    BookingRepository --|> Repository
    
    DefaultBookingService --> BookingRepository
    BookingController --> BookingService
```

### Frontend

```mermaid
classDiagram
    class BookingView {
        -slotElement: HTMLElement
        +highlightSelected(slot: Slot)
        +bindConfirm(handler: function)
        +showConfirmation(id: string)
    }
    BookingView --|> View

    class BookingPresenter {
        -view: BookingView
        -api: APIService
        +selectSlot(slot: Slot)
        +confirmBooking()
    }
    BookingPresenter --|> Presenter
    
    BookingPresenter --> BookingView
```

## Diagramma di Attività

```mermaid
flowchart TD
    Start([Inizio]) --> ViewSlots[Visualizza Slot (UC03)]
    ViewSlots --> SelectSlot[Seleziona Slot]
    SelectSlot --> CheckFree{Slot Libero?}
    CheckFree -- No --> Error[Mostra Errore]
    Error --> ViewSlots
    CheckFree -- Si --> Payment[Pagamento (UC08)]
    Payment --> PayResult{Pagamento OK?}
    PayResult -- No --> UnlockSlot[Sblocca Slot]
    UnlockSlot --> Error
    PayResult -- Si --> Save[Salva Prenotazione]
    Save --> Confirm[Mostra Conferma]
    Confirm --> End([Fine])
```

## Diagramma di Sequenza

### Frontend

```mermaid
sequenceDiagram
    autonumber
    actor User
    participant View as BookingView
    participant Presenter as BookingPresenter
    participant API as APIService
    participant Backend

    User->>View: Seleziona Slot
    View->>Presenter: selectSlot(slot)
    Presenter->>View: highlightSelected(slot)
    
    User->>View: Conferma Prenotazione
    View->>Presenter: confirmBooking()
    Presenter->>API: createBooking(slotId)
    API->>Backend: POST /booking (slotId)
    Backend-->>API: {success: true, bookingId: 123}
    API-->>Presenter: success
    Presenter->>View: showConfirmation()
```

### Backend (Create Booking)

```mermaid
sequenceDiagram
    autonumber
    participant Router
    participant Controller as BookingController
    participant Service as BookingService
    participant Repo as BookingRepository
    participant DB as Database

    Router->>Controller: resolve("create")
    Controller->>Service: createBooking(user, slot)
    Service->>Repo: isSlotFree(slot)
    Repo-->>Service: true
    Service->>Repo: save(booking)
    Repo->>DB: INSERT INTO bookings ...
    DB-->>Repo: success
    Repo-->>Service: true
    Service-->>Controller: success
    Controller-->>Router: Response 201 Created
```
