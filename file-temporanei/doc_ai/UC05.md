# UC05 - Visualizzazione Corsi

## Diagramma delle Classi

Vedi [Architettura Base](Architettura_Base.md).

### Backend
```mermaid
classDiagram
    class CourseController {
        -service: CourseService
        +listCourses()
        +getCourseDetails(id)
    }
    CourseController --|> Controller

    class CourseService {
        +getAllCourses() List~Course~
    }
    CourseService --|> Service

    class CourseRepository {
        +findAll()
    }
    CourseRepository --|> Repository

    CourseController --> CourseService
    CourseService --> CourseRepository
```

### Frontend
```mermaid
classDiagram
    class CourseListView {
        +renderList(courses)
    }
    CourseListView --|> View

    class CoursePresenter {
        +loadCourses()
    }
    CoursePresenter --|> Presenter
    
    CoursePresenter --> CourseListView
```

## Diagramma di Attività
```mermaid
flowchart TD
    Start([Inizio]) --> OpenCourses[Apri Pagina Corsi]
    OpenCourses --> ReqCards[Richiedi Lista Corsi]
    ReqCards --> Fetch[Recupera da DB]
    Fetch --> Show[Mostra Lista Cards]
    Show --> Filter{Filtra?}
    Filter -- Si --> UpdateList[Aggiorna Lista]
    UpdateList --> Show
    Filter -- No --> Detail{Vedi Dettaglio?}
    Detail -- Si --> ViewDetail[Mostra Dettagli (UC06)]
    Detail -- No --> End([Fine])
```

## Diagramma di Sequenza
```mermaid
sequenceDiagram
    autonumber
    actor User
    participant View as CourseListView
    participant Presenter as CoursePresenter
    participant API as APIService
    participant Controller as CourseController
    participant Service as CourseService
    participant Repo as CourseRepository

    User->>View: Apre pagina corsi
    View->>Presenter: init()
    Presenter->>API: getCourses()
    API->>Controller: GET /courses
    Controller->>Service: getAllCourses()
    Service->>Repo: findAll()
    Repo-->>Service: List<Course>
    Service-->>Controller: List<Course>
    Controller-->>API: JSON
    API-->>Presenter: List
    Presenter->>View: renderList(courses)
```
